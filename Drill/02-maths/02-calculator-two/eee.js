operateur = "-"                 break;             case "multiplication":                 resultat = valeurs[0] * valeurs[1]                 operateur = "*"                 break;             case "division":                 resultat = valeurs[0] / valeurs[1]                 operateur = "/"                 break;             default:                 alert("WHAT?");         }         alert(resultat)     };     Array.from(document.querySelectorAll("button.operator")).forEach($btn =>         $btn.addEventListener(             "click",             () => (performOperation($btn.id), false),         ),     );      function prendValeurs() {         let nb1 = document.querySelector("#op-one").value         let nb2 = document.querySelector("#op-two").value          nb1 = parseInt(nb1);         nb2 = parseInt(nb2)         return [nb1, nb2]     }  })();
